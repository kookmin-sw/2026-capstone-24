spring:
  datasource:
    url: jdbc:h2:mem:murang-test;MODE=MariaDB;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

app:
  security:
    jwt:
      secret: "test-secret-for-hs512-signing-must-be-at-least-sixty-four-bytes-long-2026"
      allow-ephemeral-secret: false
