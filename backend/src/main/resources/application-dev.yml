spring:
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/murang?sslMode=disable}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 5000
