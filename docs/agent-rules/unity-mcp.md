# Unity MCP 호출 규칙

> 이 파일은 Unity MCP 도구를 호출하는 작업 시에만 읽으세요.

- Unity MCP 도구를 호출하기 전에 파라미터 enum 값을 도구 스키마와 대조해 검증합니다. 검색 모드나 refresh 플래그 같은 값을 추측하지 않습니다.
- MCP 프로퍼티 setter 호출이 오브젝트 참조 할당에 실패하거나 결과를 검증하기 어려운 경우, 먼저 실제 직렬화나 패키지 코드를 확인한 뒤 필요한 최소한의 YAML만 패치합니다.
